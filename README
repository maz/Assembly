This is a toy virtual machine. It has a very limited instruction set. To run a program, run the binary as follows:
./assembly <source-file>
Note, the compiler is for a higher-level language which I hope to eventually write. You run the souce files directly.